@inherits LayoutComponentBase

<MudThemeProvider Theme="@_theme"/>
<MudDialogProvider />
<MudSnackbarProvider />

<MudLayout>

    <MudAppBar Elevation="2" Color="Color.Primary">

        <MudText Typo="Typo.h6" Class="ml-3">
            Счетоводен Калкулатор
        </MudText>

        <MudSpacer />

        <MudButton Href="/"
                   Color="Color.Inherit"
                   Variant="Variant.Text"
                   StartIcon="@Icons.Material.Filled.Home">
            Начало
        </MudButton>

        <MudButton Href="/salary"
                   Color="Color.Inherit"
                   Variant="Variant.Text"
                   StartIcon="@Icons.Material.Filled.People">
            Калкулатор Заплата
        </MudButton>

        <MudButton Href="/vat-tax"
                   Color="Color.Inherit"
                   Variant="Variant.Text"
                   StartIcon="@Icons.Material.Filled.Receipt">
            ДДС Калкулатор
        </MudButton>

        <MudButton Href="/corporate-tax"
                   Color="Color.Inherit"
                   Variant="Variant.Text"
                   StartIcon="@Icons.Material.Filled.AccountBalance">
            Корпор. данък Калкулатор
        </MudButton>

        @* <MudButton Href="/"
                   Color="Color.Inherit"
                   Variant="Variant.Text"
                   StartIcon="@Icons.Material.Filled.Summarize">
            Дневник ДДС
        </MudButton> *@

    </MudAppBar>

    <MudMainContent>
        <MudToolBar />
        <MudContainer MaxWidth="MaxWidth.ExtraLarge" Class="mt-4 pa-4">
            @Body
        </MudContainer>
    </MudMainContent>
</MudLayout>

@code{
    private MudTheme _theme = new MudTheme()
    {
        PaletteLight = new PaletteLight()
        {
            Primary = "#7E57C2",   // deep purple
            Secondary = "#546E7A",   // blue grey
            Tertiary = "#00796B",   // teal
            Success = "#2E7D32",   // dark green
            Warning = "#F57F17",   // amber
            Error = "#C62828",   // dark red
            Background = "#F5F5F5",  // light grey page background
            Surface = "#FFFFFF",   // white cards
        }
    };
}

